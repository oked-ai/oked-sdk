{
  "name": "oked-sdk-repo",
  "private": true,
  "version": "0.0.0",
  "description": "OKed SDK repository - @oked/sdk and official integrations",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "dev": "npm run dev --workspace @oked/sdk",
    "version:all": "npm version --workspaces --include-workspace-root=false",
    "publish:all": "npm publish --workspaces --access public --provenance"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oked-ai/oked-sdk.git"
  },
  "license": "MIT"
}
